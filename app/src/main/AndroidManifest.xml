<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chatbot_stiki">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Server.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Menu.ChatActivitiy"
            android:exported="false" />
        <activity android:name=".Menu.Register" />
        <activity android:name=".Menu.EditProfile" />
        <activity
            android:name=".Menu.About"
            android:parentActivityName=".Menu.Setelan" />
        <activity
            android:name=".Menu.Help"
            android:parentActivityName=".Menu.Setelan" />
        <activity android:name=".Menu.Setelan" />
        <activity
            android:name=".Menu.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Menu.MainActivity" />
    </application>

</manifest>